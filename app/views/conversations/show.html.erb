<div class="row">
    <div class="col-md-9">
        <ul id="conversations-list">
            <% @conversations.each do |conversation| %>
            <%= render 'conversations/conversation_user', conversation: conversation, user: current_user %>
            <% end %>
        </ul>
    </div>
</div>